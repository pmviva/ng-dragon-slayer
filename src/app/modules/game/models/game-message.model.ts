/**
 * Defines the game message interface.
 */
export interface GameMessage {
  /**
   * Defines the key of the game message.
   */
  key: string;

  /**
   * Defines the params of the game message.
   */
  params: object;
}
